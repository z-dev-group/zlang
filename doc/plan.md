use golang develop first release
use llvm and z self develop next release

function list：

* variable type (int, float, bool, string, array, map)
* function
* if
* while
* import
* builtin function

todo list:

* oop
