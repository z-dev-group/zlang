import "../standard/array.z"
let names = ["jack", "tony", "sevenpan"]
let index = 0
while (index < len(names)) {
  let name = names[index]
  puts("current name is:", name, "\n")
  index = index + 1
}

var_dump(join(names, "','"))

let ages = [1, 2, 3, 4]
var_dump(ages)
var_dump(ages[1 + 1])
ages[1 + 1]