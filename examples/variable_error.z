let age = 12
with_error(age, "this is test error")
var_dump(age)
if (is_with_error(age)) {
  puts("has error", "\n")
  puts("get error message is:" + get_error_message(age), "\n")
}

let name = "sevenpan"
with_error(name, "error with name")
if (is_with_error(name)) {
  var_dump(get_error_message(name))
}